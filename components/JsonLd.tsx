/**
 * Organization + LocalBusiness JSON-LD is deferred until {{TBD-ADDRESS}}
 * resolves (SPEC_V1.md §3). Do not emit streetAddress / aggregateRating.
 * See DECISIONS.md Phase F.
 */
export function LocalBusinessJsonLd() {
  return null;
}
